export enum UserQueueJobProcess {
  CHECK_RANK_USER = 'checkRankUser',
  CHECK_SPLIT_TREE = 'checkSplitTree',
}

export enum UserMsg {
  USER_NOT_FOUND = 'User not found',
}

export enum BinaryChildType {
  LEFT = 1,
  RIGHT = 2,
}
